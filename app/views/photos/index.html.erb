<div class="container">
  <div class="photo_box_wrapper">

      <% @photos.each do |photo| %>
          <div class="photo_box">
            <%= link_to (image_tag photo.url), photo.url, target: '_blank' %>
            </div>
        <% end %>

    </div>
</div>
